# Codeforces Solutions

这是我用来存放 Codeforces 题解的仓库，所有文件都是独立的 C++23 源码（主要在 `Div3/` 目录），方便随时编译运行。仓库里自带一个简单版的 `bits/stdc++.h`，只是为了让常用的 `#include <bits/stdc++.h>` 在任何编译器上都能工作。

没有额外脚本或复杂结构——挑一个文件，用 `-std=c++23` 编译就行。

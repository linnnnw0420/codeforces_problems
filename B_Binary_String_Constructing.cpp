#include<bits/stdc++.h>
using namespace std;
#define ll long long

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int a, b, x;
    cin >> a >> b >> x;
    for (int i = 0; i < a + b; ++i) {
        if (x > 0){
            
        }
    }
    return 0;
}